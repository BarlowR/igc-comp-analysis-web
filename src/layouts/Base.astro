---
// Shared page shell: head, fonts, global styles, and top nav.
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Comp Tracklog Analysis',
  description = 'Analyze and archive paragliding competition flights in your browser.',
} = Astro.props;

const path = Astro.url.pathname;
const isArchive = path.startsWith('/archive');
const isHome = path === '/';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;200;300;400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav class="topnav">
      <a href="/" class:list={[{ active: isHome }]}>Analyze</a>
      <a href="/archive" class:list={[{ active: isArchive }]}>Archive</a>
      {/* Filled in by src/scripts/nav-account.ts — the session only exists client-side. */}
      <span id="nav-account" class="nav-account"></span>
    </nav>

    <slot />

    <script>
      import '../scripts/nav-account';
    </script>
  </body>
</html>
